% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{fg_strat_boot}
\alias{fg_strat_boot}
\title{Bootstrap Fine-Gray coefficients from stratified FG weights}
\format{
A numeric matrix with 500 rows (bootstrap replicates) and 3
columns (\code{z1} levels 1, 2, and 3 vs. the reference level 0).
}
\source{
Generated by the authors by applying \code{\link[=get_ipcw_boot_cr]{get_ipcw_boot_cr()}} with
\code{B = 500} and \code{seed = 20240917} to a competing risks dataset simulated
via \code{sim_data_cr(n = 500, censoring = "baseline", beta1 = log(1.5), beta2 = log(2.25), beta3 = log(3.4), p = 0.3)} with \code{set.seed(9843)},
then fitting \code{\link[=fg_weighted_cr]{fg_weighted_cr()}} with stratified weights
(\code{\link[=add_fg_weights_cr]{add_fg_weights_cr()}} with \code{strat = "yes"}) to each bootstrap sample and
extracting the coefficient column.
}
\usage{
data(fg_strat_boot)
}
\description{
A 500-by-3 matrix of bootstrap log sub-distribution hazard ratio estimates
for the effect of baseline PSA quartile (\code{z1}) on metastasis, each row
obtained by fitting a stratified-weight Fine-Gray model to one bootstrap
sample of the competing risks guided example data. Provided as package
data to avoid re-running the time-consuming bootstrap in vignettes and
examples. Use with \code{\link[=get_boot_pci_cr]{get_boot_pci_cr()}} to obtain bootstrap-based
confidence intervals.
}
\keyword{datasets}
