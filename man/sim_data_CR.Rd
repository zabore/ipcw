% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/competing-risks.R
\name{sim_data_CR}
\alias{sim_data_CR}
\title{Simulate competing risks survival data}
\usage{
sim_data_CR(
  n = 100,
  censoring = "none",
  beta1 = log(1.5),
  beta2 = log(2.25),
  beta3 = log(3.4),
  p = 0.3
)
}
\arguments{
\item{n}{Integer. Number of subjects to simulate. Default is 100.}

\item{censoring}{Character string specifying the censoring mechanism.
One of \code{"none"}, \code{"independent"}, or \code{"baseline"} (censoring rate depends
on \code{z1}). Default is \code{"none"}.}

\item{beta1}{Numeric. Log sub-distribution hazard ratio for \code{z1 = 1} vs
\code{z1 = 0} for event type 1. Default is \code{log(1.5)}.}

\item{beta2}{Numeric. Log sub-distribution hazard ratio for \code{z1 = 2} vs
\code{z1 = 0} for event type 1. Default is \code{log(2.25)}.}

\item{beta3}{Numeric. Log sub-distribution hazard ratio for \code{z1 = 3} vs
\code{z1 = 0} for event type 1. Default is \code{log(3.4)}.}

\item{p}{Numeric in (0, 1). Baseline probability of event type 1 in the
reference group (\code{z1 = 0}). Default is \code{0.3}.}
}
\value{
A data frame with columns:
\describe{
\item{z1}{Four-level factor covariate (levels 0-3).}
\item{delta}{Factor event indicator with levels \code{"censor"}, \code{"event_1"},
\code{"event_2"}.}
\item{t}{Event or censoring time.}
}
}
\description{
Generates a dataset with two competing events under a sub-distribution
hazard model parameterized by a four-level baseline covariate \code{z1}.
Optionally adds independent or covariate-dependent administrative censoring.
}
\examples{
set.seed(42)
dat <- sim_data_CR(n = 200, censoring = "baseline")
table(dat$delta)

}
