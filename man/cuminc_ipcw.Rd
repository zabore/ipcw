% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/competing-risks.R
\name{cuminc_ipcw}
\alias{cuminc_ipcw}
\title{Cox model IPCW cumulative incidence estimate}
\usage{
cuminc_ipcw(
  data_long,
  esttimes = seq(from = 0, to = 10, length.out = 100),
  extend = TRUE,
  covariate = "z1"
)
}
\arguments{
\item{data_long}{A long-format data frame with IPCW weights, as returned by
\code{\link[=add_ipcw_weights]{add_ipcw_weights()}}. Must contain columns \code{tstart}, \code{tstop}, \code{delta},
\code{id}, and \code{p_notcens}.}

\item{esttimes}{Numeric vector of times at which to return estimates.
Defaults to 100 equally spaced points from 0 to 10.}

\item{extend}{Logical. If \code{FALSE}, estimates beyond the minimum of the
stratum-specific maximum follow-up times are set to \code{NA}. Default is
\code{TRUE}.}

\item{covariate}{Character string. Name of the covariate column, used only
when \code{extend = FALSE} to compute the truncation time. Default is \code{"z1"}.}
}
\value{
A numeric vector of IPCW cumulative incidence estimates at
\code{esttimes}.
}
\description{
Estimates the marginal cumulative incidence of event type 1 using a weighted
Aalen-Johansen estimator, where the weights are the IPCW weights stored in
the \code{p_notcens} column (as added by \code{\link[=add_ipcw_weights]{add_ipcw_weights()}}).
}
\examples{
set.seed(42)
dat <- sim_data_cr(n = 200, censoring = "baseline")
dat_long <- wide_to_long_cr(dat)
dat_long <- add_ipcw_weights(dat_long, strat = "no")
cuminc_ipcw(dat_long, esttimes = seq(0, 5, 0.5))

}
