% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/competing-risks.R
\name{get_ipcw_boot_cr}
\alias{get_ipcw_boot_cr}
\title{Bootstrap IPCW weighted data for competing risks survival analysis}
\usage{
get_ipcw_boot_cr(
  data,
  B = 500,
  time_var = "t",
  event_var = "delta",
  covariate = "z1",
  cens_level = "censor",
  event2_level = "event_2",
  strat = "no",
  seed = NULL
)
}
\arguments{
\item{data}{A wide-format competing risks data frame containing the
columns specified by \code{time_var}, \code{event_var}, and \code{covariate}.}

\item{B}{Integer. Number of bootstrap samples. Default is \code{500}.}

\item{time_var}{Character string. Name of the event/censoring time column.
Default is \code{"t"}.}

\item{event_var}{Character string. Name of the event indicator column.
Default is \code{"delta"}.}

\item{covariate}{Character string. Name of the covariate column. Default
is \code{"z1"}.}

\item{cens_level}{Character string. Factor level in \code{event_var}
representing censoring. Default is \code{"censor"}.}

\item{event2_level}{Character string. Factor level in \code{event_var}
representing the competing event (event type 2). Default is
\code{"event_2"}.}

\item{strat}{Character. Passed to \code{\link[=add_ipcw_weights_cr]{add_ipcw_weights_cr()}}. \code{"no"}
(default) fits a single Cox model for the censoring distribution;
\code{"yes"} estimates the censoring distribution non-parametrically within
each stratum of \code{covariate}.}

\item{seed}{Optional integer seed for reproducibility. Default is \code{NULL}.}
}
\value{
A list of \code{B} data frames, each in long (counting-process) format
with an IPCW weight column \code{p_notcens}, as returned by
\code{\link[=add_ipcw_weights_cr]{add_ipcw_weights_cr()}}.
}
\description{
Draws \code{B} bootstrap samples from the original wide-format competing risks
data, converts each sample to long (counting-process) format via
\code{\link[=wide_to_long_cr]{wide_to_long_cr()}}, and appends IPCW weights via \code{\link[=add_ipcw_weights_cr]{add_ipcw_weights_cr()}}.
Returns the results as a list of long-format data frames that can be used
directly with \code{\link[=cuminc_ipcw_cr]{cuminc_ipcw_cr()}}, or further processed with
\code{\link[=fg_split_cr]{fg_split_cr()}} and \code{\link[=add_fg_weights_cr]{add_fg_weights_cr()}} for a Fine-Gray bootstrap.
}
\examples{
\dontrun{
set.seed(42)
dat <- sim_data_cr(n = 200, censoring = "baseline")
boot_list <- get_ipcw_boot_cr(dat, B = 50)
}

}
