% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/single-event.R
\name{get_ipcw_boot}
\alias{get_ipcw_boot}
\title{Bootstrap IPCW weighted data for single-event survival analysis}
\usage{
get_ipcw_boot(
  data,
  B = 500,
  time_var = "t",
  event_var = "delta",
  cens_cov = "W2",
  seed = NULL
)
}
\arguments{
\item{data}{A wide-format data frame containing the columns specified by
\code{time_var}, \code{event_var}, and \code{cens_cov}.}

\item{B}{Integer. Number of bootstrap samples. Default is \code{500}.}

\item{time_var}{Character string. Name of the observed follow-up time column.
Default is \code{"t"}.}

\item{event_var}{Character string. Name of the event indicator column
(1 = event, 0 = censored). Default is \code{"delta"}.}

\item{cens_cov}{Character string. Name of the covariate column used to model
the censoring distribution. Default is \code{"W2"}.}

\item{seed}{Optional integer seed for reproducibility. Default is \code{NULL}.}
}
\value{
A list of \code{B} data frames, each in long (counting-process) format
as returned by \code{\link[=get_ipcw_wgt]{get_ipcw_wgt()}}.
}
\description{
Draws \code{B} bootstrap samples from the original wide-format data, fits IPCW
weights to each sample via \code{\link[=get_ipcw_wgt]{get_ipcw_wgt()}}, and returns the results as a
list of long-format data frames. The list can be passed to
\code{\link[=plot_ipcw_km_boot_ci]{plot_ipcw_km_boot_ci()}} for plotting or used directly to bootstrap other
quantities (e.g. Cox hazard ratios via \code{\link[=get_ipcw_cox_fit]{get_ipcw_cox_fit()}}).
}
\examples{
\dontrun{
set.seed(1)
dat <- sim_data_SE(n = 200)
boot_list <- get_ipcw_boot(dat, B = 50)
}

}
