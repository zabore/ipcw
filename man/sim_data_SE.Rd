% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/single-event.R
\name{sim_data_SE}
\alias{sim_data_SE}
\title{Simulate single-event survival data with informative censoring}
\usage{
sim_data_SE(
  n = 500,
  alpha = 0.05,
  x_prop = 0.5,
  a = 2,
  sigma = 500,
  beta = log(0.25),
  lambda = 0.01,
  phi = -5
)
}
\arguments{
\item{n}{Integer. Number of subjects to simulate. Default is 500.}

\item{alpha}{Numeric. Shape parameter for the gamma frailty distribution,
also used as the Pareto exponent for \code{W1} and \code{W2}. Default is \code{0.05}.}

\item{x_prop}{Numeric in (0, 1). Probability of treatment (\code{x = 1}).
Default is \code{0.5}.}

\item{a}{Numeric. Weibull shape parameter for the event time distribution.
Default is \code{2}.}

\item{sigma}{Numeric. Weibull scale parameter for the event time
distribution. Default is \code{500}.}

\item{beta}{Numeric. Log hazard ratio for the treatment effect on event
time. Default is \code{log(0.25)}.}

\item{lambda}{Numeric. Baseline censoring rate. Default is \code{0.01}.}

\item{phi}{Numeric. Effect of \code{W2} on the censoring rate (log scale).
Negative values mean higher \code{W2} leads to a lower censoring rate
(i.e., longer follow-up for high-\code{W2} subjects). Default is \code{-5}.}
}
\value{
A data frame with columns:
\describe{
\item{S}{True (latent) event time.}
\item{t}{Observed time (minimum of \code{S} and the censoring time).}
\item{delta}{Event indicator: 1 = event occurred, 0 = censored.}
\item{x}{Binary treatment indicator (0 or 1).}
\item{W2}{Ancillary biomarker covariate that drives informative
censoring.}
}
}
\description{
Generates a dataset where event times follow a Weibull distribution
parameterized by a binary treatment covariate \code{x}, and censoring depends
on an ancillary biomarker \code{W2} through a shared gamma frailty, inducing
informative censoring. This is the data-generating mechanism used in the
single-event guided example.
}
\examples{
set.seed(20240429)
dat <- sim_data_SE(n = 500)
table(dat$delta)
summary(dat$t)

}
