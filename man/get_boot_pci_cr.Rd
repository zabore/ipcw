% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/competing-risks.R
\name{get_boot_pci_cr}
\alias{get_boot_pci_cr}
\title{Compute bootstrap percentile confidence intervals for competing risks estimates}
\usage{
get_boot_pci_cr(boot_mat)
}
\arguments{
\item{boot_mat}{A numeric matrix of bootstrap estimates, with one row per
bootstrap replicate and one column per estimand (e.g. time point or
regression term), as returned by row-binding the results of multiple
calls to \code{\link[=cuminc_waverage_cr]{cuminc_waverage_cr()}}, \code{\link[=cuminc_ipcw_cr]{cuminc_ipcw_cr()}}, or \code{\link[=fg_weighted_cr]{fg_weighted_cr()}}.}
}
\value{
A 2-row numeric matrix with rows \code{"lower"} and \code{"upper"} giving the
2.5th and 97.5th percentiles of the bootstrap distribution for each
column of \code{boot_mat}. Columns with any \code{NA} bootstrap estimate return
\code{NA} for both bounds.
}
\description{
Computes the 2.5th and 97.5th percentiles of the bootstrap distribution for
each column of a matrix of bootstrap estimates. Intended for competing
risks quantities (e.g. cumulative incidence at a set of time points, or
Fine-Gray regression terms), where each bootstrap replicate produces more
than one estimate, unlike the single scalar log hazard ratio handled by
\code{\link[=get_boot_pci_se]{get_boot_pci_se()}}.
}
\examples{
set.seed(1)
boot_mat <- matrix(rnorm(500 * 3), nrow = 500, ncol = 3)
get_boot_pci_cr(boot_mat)

}
